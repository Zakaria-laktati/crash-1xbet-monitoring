# Configuration Docker pour 1xBet Crash Monitoring
# Copiez ce fichier en .env et configurez les valeurs

# =============================================================================
# TOKEN 1XBET (Optionnel - priorité au config.yaml)
# =============================================================================
# XBET_ACCESS_TOKEN=votre_token_ici

# =============================================================================
# CONFIGURATION BASE DE DONNÉES
# =============================================================================
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=crash_db
DATABASE_USER=crash_user
DATABASE_PASSWORD=crash_password_2025

# =============================================================================
# TOKEN MONITOR
# =============================================================================
# Intervalle de vérification du token en secondes (défaut: 600 = 10 minutes)
CHECK_INTERVAL=600

# =============================================================================
# NOTES
# =============================================================================
# 1. Le token 1xBet doit être configuré dans config/config.yaml
# 2. Pour mettre à jour le token: python update_token.py
# 3. Le token expire généralement après 1-4 heures
